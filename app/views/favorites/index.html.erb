<% @favorites.each do |favorite| %>
  <div>
    <%= image_tag favorite.recipe.image_url %>
    <%= favorite.recipe.title %>
    <%= favorite.recipe.user.name %>
    <%= favorite.recipe.created_at.strftime('%Y年%m月%d日') %>
  </div>
<% end %>
